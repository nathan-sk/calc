#include <string>

#ifndef UTILS_H
#define UTILS_H

bool isError( double number1 = 0, double number2 = 0);
bool isDouble( const std::string& string );
void printError( const std::string& error );

#endif
